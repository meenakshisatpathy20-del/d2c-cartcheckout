{
  "name": "d2c-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd backend && node server.js",
    "client": "cd frontend && npm run dev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"yellow,cyan\" \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}